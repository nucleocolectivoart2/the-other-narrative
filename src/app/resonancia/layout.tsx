
import { Metadata } from 'next';

export const metadata: Metadata = {
  title: 'Capítulo 04: Resonancia Auditiva',
  description: 'Planeta Sostenible: Humanizando la técnica a través de la escucha profunda y el diálogo territorial.',
};

export default function ResonanciaLayout({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}
